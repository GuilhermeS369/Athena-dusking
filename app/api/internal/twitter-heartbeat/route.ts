import { NextResponse } from 'next/server';import { createSupabaseAdminClient } from '@/lib/supabase/admin';import { isTwitterWorkerAuthorized } from '@/lib/twitter/worker-auth';
const allowed=new Set(['athena-twitter-publication-worker','athena-twitter-generation-worker','athena-twitter-zernio-sync-worker','athena-twitter-analytics-worker','athena-twitter-webhook-reconcile-worker']);
export async function POST(request:Request){if(!isTwitterWorkerAuthorized(request))return NextResponse.json({error:'Não autorizado.'},{status:401});const body=await request.json().catch(()=>({})) as {workerName?:unknown;workerId?:unknown;metadata?:unknown};if(typeof body.workerName!=='string'||!allowed.has(body.workerName)||typeof body.workerId!=='string')return NextResponse.json({error:'Worker X inválido.'},{status:400});const{error}=await createSupabaseAdminClient().rpc('twitter_record_worker_heartbeat',{p_worker_name:body.workerName,p_worker_id:body.workerId,p_mode:body.workerName.includes('publication')&&process.env.TWITTER_PUBLICATION_WORKER_ENABLED==='true'?'shadow':'stopped',p_metadata:body.metadata??{}});if(error)return NextResponse.json({error:'Falha no heartbeat X.'},{status:500});return NextResponse.json({ok:true});}
