'use client';import { useState } from 'react';
export function TwitterLogResolution({attemptId}:{attemptId:string}){const[j,setJ]=useState('');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');async function resolve(decision:'published'|'confirmed_failure'){setBusy(true);setMessage('');const response=await fetch('/api/x/logs/resolve',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({attemptId,decision,justification:j})});const payload=await response.json().catch(()=>({}));setMessage(response.ok?'Ocorrência resolvida. Recarregue a página.':payload.error??'Falha.');setBusy(false);}return <div className="content-stack"><textarea value={j} onChange={e=>setJ(e.target.value)} placeholder="Justificativa obrigatória e evidência conferida"/><div className="action-row"><button disabled={busy||j.trim().length<8} onClick={()=>resolve('published')}>Confirmar publicado/cobrado</button><button disabled={busy||j.trim().length<8} onClick={()=>resolve('confirmed_failure')}>Confirmar falha/não cobrado</button></div>{message?<p>{message}</p>:null}</div>}
