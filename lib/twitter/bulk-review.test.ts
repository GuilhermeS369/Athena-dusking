import assert from 'node:assert/strict'; import test from 'node:test'; import { buildTwitterBulkReview } from './bulk-review.ts';
test('review financia parcialmente sem materializar excedente',()=>{ const result=buildTwitterBulkReview({profiles:[{id:'p1',connectionId:'c1',epochId:'e1',identityId:'w1',username:'a',tier:'free'}],wallets:[{identityId:'w1',postedMicros:30000,reservedMicros:0,version:1}],texts:['barato','https://exemplo.com caro'],mediaSets:[],schedule:{kind:'interval',startsAt:new Date(Date.now()+60000).toISOString(),intervalMinutes:1,durationMinutes:1000}}); assert.equal(result.fundedCount,2); assert.equal(result.unfundedCount,999); assert.equal(result.items.length,2); assert.equal(result.reservedMicros,30000); });
