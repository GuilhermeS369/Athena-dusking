import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';
test('detalhe de perfil X usa identidade estável e histórico exclusivamente local',async()=>{const[list,detail]=await Promise.all([readFile(new URL('../../app/(painel)/x/perfis/page.tsx',import.meta.url),'utf8'),readFile(new URL('../../app/(painel)/x/perfis/[profileId]/page.tsx',import.meta.url),'utf8')]);assert.match(list,/\/x\/perfis\/\$\{profile\.id\}/);for(const table of['twitter_profiles','twitter_profile_connection_epochs','twitter_connections','twitter_group_members','twitter_publication_items','twitter_analytics_snapshots'])assert.match(detail,new RegExp(table));assert.match(detail,/Abrir Análises X/);assert.match(detail,/Sem leitura automática/);assert.doesNotMatch(`${list}\n${detail}`,/instagram_profiles|\/v1\/|zernio\.com/);});
